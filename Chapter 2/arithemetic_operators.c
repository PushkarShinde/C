#include <stdio.h>

int main() {
    int a = 10;
    int b = 33;
    int c = a + b;
    printf("The value of a is:  %d, the value of b is: %d, and the vlue of c is: %d\n", a, b, c);
    printf("The vlue of remainder when b is divided by a is: %d\n", b%a);
    return 0;
} 