#include <stdio.h>

int main() {
    int i =0;
    do
    {
        printf("The value of i is %d\n",i);
        i++;
    } while (i<4);
    // Do while loop executes atleast once always.
    return 0;
}